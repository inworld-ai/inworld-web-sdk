name: build-package

runs:
  using: "composite"
  steps:
    - name: Setup node
      uses: actions/setup-node@v3
      with:
        node-version: '18.x'
    - name: Install dependencies
      shell: bash
      run: yarn install
    - name: Build
      shell: bash
      run: yarn build
